# Full replacement for RNN.ipynb Cell In[16] - PASTE THIS DIRECTLY&#10;# Fixes AttributeError by proper model building + safe input access&#10;&#10;import numpy as np&#10;from tensorflow import keras&#10;from tensorflow.keras import layers&#10;&#10;# SAFE BUILD - works even if already built&#10;try:&#10;    if not model_lstm.built:&#10;        model_lstm.build((None, 200, 1))&#10;    print("Model built OK")&#10;except Exception as e:&#10;    print(f"Build issue: {e}. Creating new extractor.")&#10;&#10;# CREATE ACTIVATION MODEL&#10;lstm_layer = model_lstm.layers[0]&#10;activation_model = keras.Model(inputs=model_lstm.input, outputs=lstm_layer.output)&#10;&#10;# TEST WITH DUMMY DATA&#10;X_dummy = np.random.rand(1, 200, 1)&#10;acts = activation_model.predict(X_dummy, verbose=0)&#10;&#10;print(f"✅ FIXED SUCCESS! Shape: {acts.shape}")&#10;print(f"Last hidden state: {acts[0,-1,:5]}")&#10;&#10;# FULL SEQUENCE LSTM RECONSTRUCT&#10;full_seq_lstm = keras.Sequential([&#10;    layers.LSTM(64, input_shape=(200,1), return_sequences=True, name=&#39;lstm_viz&#39;)&#10;])&#10;print("Full sequence visualization model ready")&#10;# full_seq_lstm.summary()
